# ---------------------------------------------------------------------
# THIS FILE IS UNPINNED AND IS NOT WHAT CI INSTALLS.
#
# Install requirements.lock instead:   pip install -r requirements.lock
#
# It is kept only as the human-readable statement of WHICH packages the
# project needs and why. The pandas-3 lesson: an unpinned major bumped
# underneath us and behaviour moved with no commit to point at.
# ---------------------------------------------------------------------

# Core — required for signal_engines.py / pipeline.py / live_tools.py check mode
requests
pandas
numpy
yfinance

# ML confidence engine (pipeline.py run --ml, or live_tools.py check --confluence)
xgboost
scikit-learn

# NOTE: plyer and winsound (desktop alerts/sound) are intentionally NOT listed
# here — they're only used by `live_tools.py monitor`, which is for a machine
# you keep running locally. GitHub Actions is headless, so `live_tools.py
# check` (used by the workflow below) never touches them.
